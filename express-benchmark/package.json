{
  "name": "express-benchmark",
  "version": "1.0.0",
  "description": "Benchmark comparing traditional Express routing vs Rust proxy routing",
  "scripts": {
    "start:backend": "node test-route.js",
    "start:proxy": "cd .. && cargo run --bin rust-router",
    "test": "node test-proxy.js",
    "test:full": "concurrently \"npm run start:backend\" \"sleep 2 && npm run start:proxy\" \"sleep 4 && npm run test\""
  },
  "dependencies": {
    "axios": "^1.6.2",
    "express": "^4.18.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
